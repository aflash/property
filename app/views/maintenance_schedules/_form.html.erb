<%= form_for(@maintenance_schedule) do |f| %>
  <% if @maintenance_schedule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@maintenance_schedule.errors.count, "error") %> prohibited this maintenance_schedule from being saved:</h2>

      <ul>
      <% @maintenance_schedule.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :schedule_type %><br />
    <%= f.text_field :schedule_type %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :schedule_date %><br />
    <%= f.date_select :schedule_date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
